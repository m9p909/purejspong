{
  "devDependencies": {
    "typescript": "^4.8.4"
  },
  "scripts": {
    "build": "esbuild ./src/*.ts --bundle --outdir=build",
    "dev": "esbuild ./src/*.ts --bundle  --outdir=build --watch"
  },
  "dependencies": {
    "esbuild": "^0.15.10"
  }
}
